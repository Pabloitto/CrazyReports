(function(){

	 var LoginController = function ($scope) {

        function init() {
            bindEvents();
        }

        function login() {
            
        }

        //Attach events
        function bindEvents() {

        }

        init();
    }

    $.App.CrazyReports.controller('LoginController', ['$scope', LoginController]);

}());